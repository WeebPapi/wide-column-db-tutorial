# Demo Data

The application generates fictional deterministic event data from a numeric seed. No real personal data is used.

Use `python scripts/generate_data.py 42 small` to export a CSV copy for inspection. In the normal workshop flow, students load data from the web app.
